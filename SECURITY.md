# 安全策略

感谢你帮助提升 Veridex 的安全性。我们认真对待每一个安全漏洞报告。

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| `main` 分支（最新提交） | ✅ 支持 |

该项目尚未发布正式版本。安全修复会直接提交到 `main` 分支。

## 报告漏洞

**请不要通过公开 issue 报告安全漏洞**，以免在修复前泄露可利用的细节。

请使用 GitHub 的私有安全通告功能报告：

1. 进入仓库的 **Security** 标签页
2. 点击 **Report a vulnerability**（报告漏洞）
3. 描述漏洞并提交

这样报告会以私有形式通知维护者，我们会在修复并发布后再公开。

## 报告应包含的内容

为便于快速定位和复现，请尽量提供：

- 漏洞类型与影响范围
- 受影响的组件或版本（commit hash）
- 复现步骤或最小化 PoC
- 你建议的修复方向（如有）

## 处理流程

维护者会在收到报告后：

1. 确认收到，并评估漏洞的真实性与严重程度
2. 在私有分支中开发修复
3. 与报告者协调披露时间
4. 发布修复后公开相关细节

## 安全最佳实践

部署 Veridex 时请注意：

- 不要使用 `deploy/compose/.env.example` 或 README 中的默认开发凭据，生产环境必须替换为强密码。
- 真实模型（deepseek/ollama）的 API key 与访问凭据只通过环境变量或 Kubernetes Secret 注入，**不要提交到仓库**。
- 生产环境务必开启 Session Cookie 的 `Secure` 标记与 `VERIDEX_ENVIRONMENT` 非 `local` 值。
- 默认只放行最小出站目标（`VERIDEX_OUTBOUND_ALLOWED_HOSTS` / `_PORTS`），不要开放 `0.0.0.0/0`。
